<section class="section container-fluid">
  <h1 class="title">Blog Index</h1>
  <% @posts.each do |post| %>
    <div class="card">
      <h2 class="card-title">
        <%= post.title %>
      </h2>
      <p class="card-text">
        <%= post.content %>
      </p>
    </div>
  <% end %>
</section>